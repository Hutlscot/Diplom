<Window x:Class="Diplom.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Diplom"
        mc:Ignorable="d"
         Title="Система регистрации студентов в общежитии ТТИТ" Height="550" Width="1000" WindowStartupLocation="CenterScreen" Background="{StaticResource Back}" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Panel.ZIndex="999" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Content="{Binding ElementName=frame, Path=Content.Title}"/>
        <Frame Name="frame" NavigationUIVisibility="Hidden" Grid.Column="1" Grid.Row="1"></Frame>
        <!--<Border BorderBrush="Wheat" BorderThickness="0 0 0 2" >-->
        <materialDesign:ColorZone Mode="PrimaryMid" Grid.ColumnSpan="2" HorizontalAlignment="Stretch">
            <Grid>
                
                <materialDesign:PopupBox Name="btm_back" PlacementMode="BottomAndAlignRightEdges" HorizontalAlignment="Right">
                    <materialDesign:PopupBox.ToggleContent>
                        <Grid Panel.ZIndex="999999" MouseDown="btm_back_MouseDown">
                            <materialDesign:PackIcon Margin="14" Kind="KeyboardBackspace"/>
                        </Grid>
                    </materialDesign:PopupBox.ToggleContent>
                </materialDesign:PopupBox>
            </Grid>
            </materialDesign:ColorZone>
        <!--</Border>-->
        <Grid>
            <Image Source="unnamed.jpg" Width="46" HorizontalAlignment="Left"></Image>
            <TextBlock FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center">
                Томский техникум<LineBreak></LineBreak>информационных технологий
            </TextBlock>
        </Grid>
        <Grid HorizontalAlignment="Stretch" Grid.Row="1" Background="{StaticResource PrimaryHueMidBrush}">
            <!--<Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
                       
            <Grid Grid.Row="0" Background="{StaticResource Back}">
                
            </Grid>-->
            <ScrollViewer HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Grid.Row="1">
                <!--<Border BorderThickness="0 1 0 0" BorderBrush="Wheat">-->
                <StackPanel x:Name="Menu" Margin="10"/>
                <!--</Border>-->
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
